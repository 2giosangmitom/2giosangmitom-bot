{
  "name": "2giosangmitom-bot",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build --packages=external --production --target=bun --outdir=dist src/index.ts src/commands/* src/events/*",
    "dev": "bun --watch .",
    "format": "prettier --write .",
    "test": "vitest run",
    "test:dev": "vitest"
  },
  "devDependencies": {
    "@types/bun": "^1.2.19",
    "@types/node": "^24.0.15",
    "prettier": "^3.6.2",
    "vitest": "^3.2.4"
  },
  "peerDependencies": {
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "discord.js": "^14.21.0",
    "fuse.js": "^7.1.0",
    "pino": "^9.7.0",
    "pino-pretty": "^13.0.0"
  }
}
